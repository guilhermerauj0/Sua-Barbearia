# ============================================
# VARIÁVEIS DE AMBIENTE - EXEMPLO
# ============================================
# Copie este arquivo para .env e ajuste os valores
# Comando: cp .env.example .env

# ============================================
# PERFIL DA APLICAÇÃO
# ============================================
# dev: desenvolvimento local (mostra SQL, logs detalhados)
# prod: produção (otimizado, logs reduzidos)
SPRING_PROFILES_ACTIVE=dev

# ============================================
# SERVIDOR
# ============================================
# Render usa PORT automaticamente
# Local pode usar SERVER_PORT ou deixar padrão 8080
# PORT=8080
SERVER_PORT=8080

# ============================================
# BANCO DE DADOS - DESENVOLVIMENTO LOCAL
# ============================================
# Docker local: use DB_PORT=5433 (mapeado para 5432 do container)
# PostgreSQL local: use DB_PORT=5432
DB_HOST=localhost
DB_PORT=5433
DB_NAME=sua_barbearia
DB_USER=postgres
DB_PASSWORD=postgres

# ============================================
# BANCO DE DADOS - PRODUÇÃO (RENDER)
# ============================================
# Render fornece DATABASE_URL automaticamente no formato:
# postgresql://user:password@host:port/database
# Descomente e configure apenas se NÃO estiver no Render:
# DATABASE_URL=postgresql://user:password@host:5432/database

# ============================================
# JWT
# ============================================
# IMPORTANTE: Use uma chave forte em produção (mínimo 256 bits)
# Gere uma chave segura: openssl rand -base64 64
JWT_SECRET=sua-chave-secreta-aqui-minimo-256-bits-change-in-production
JWT_EXPIRATION=3600000

# ============================================
# TWILIO (NOTIFICAÇÕES WHATSAPP)
# ============================================
# Obtenha em: https://console.twilio.com/
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token_here
TWILIO_WHATSAPP_FROM=+14155238886
